import { MusicInterface } from '../../interfaces/music.interface';
import { albumIdStub } from './album-id.stub';

export const musicStub: MusicInterface = {
  album: '토스앨범',
  albumId: albumIdStub,
  singer: '토스',
  name: '토스',
  ranking: 1,
};
