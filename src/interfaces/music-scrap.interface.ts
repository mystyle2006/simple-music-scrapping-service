export interface MusicScrapInterface {
  url: string;
  target: string;
  rankTarget: string;
  nameTarget: string;
  singerTarget: string;
  albumTarget: string;
  albumIdTarget: string;
  albumIdPrefix: string;
}
